# Template for the required .env file. 
# Model here
MODEL_NAME=changeme

# API ENDPOINT URL
API_BASE_URL=changeme

# APIKEY here
API_KEY=changeme

# embedding model here
EMBEDDING_MODEL_NAME=changeme 

# Embedding settings:
EMBEDDING_CHUNK_SIZE=256
EMBEDDING_CHUNK_OVERLAP=64

# Embedding settings:
EMBEDDING_CHUNK_SIZE=256
EMBEDDING_CHUNK_OVERLAP=64

# Check possible JWT algorithms:
JWT_ALGORITHM=HS256
# A pseudorandom string as secret, min. 32 bytes recommended:
JWT_SECRET_KEY=changeme

# Session settings:
MAX_SESSIONS = 64
SESSION_TTL_SECONDS = 1800
SESSION_MAX_MESSAGES = 50
